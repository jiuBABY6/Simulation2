# 运行输出

`run_demo.py` 默认在此目录生成：

- `events_*.jsonl`：事件溯源日志；
- `summary_*.json`：单次运行摘要；
- `comparison_*.json`：策略对比结果；
- `comment_mapping.json`：可选的评论到群体映射结果。

这些文件是动态模拟数据，可以删除后重新运行；`config/scenario.json` 是不可变场景配置。
